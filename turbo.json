{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [".env.local", ".env.*.local"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "*.ts", "*.tsx", "*.js", "*.json"]
    },
    "test": {
      "inputs": ["src/**/*.test.{ts,tsx}", "src/**/*.{ts,tsx}", "vitest.config.ts"],
      "outputs": []
    },
    "test:coverage": {
      "inputs": ["src/**/*.test.{ts,tsx}", "src/**/*.{ts,tsx}", "vitest.config.ts"],
      "outputs": ["coverage/**"]
    }
  }
}
