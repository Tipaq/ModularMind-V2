FROM python:3.12-slim

RUN pip install --no-cache-dir fastmcp httpx

WORKDIR /connector

# Generated connector code is mounted as a volume at /connector/
# The server.py uses mcp.run(transport="sse", port=9100)

EXPOSE 9100

CMD ["python", "server.py"]
