# =============================================================================
# ModularMind V2 — Platform Environment (Owner's home server)
# =============================================================================

# --- Domain (Traefik routes HTTPS traffic to this host) ---
PLATFORM_DOMAIN=platform.example.com

# --- Database ---
DB_USER=modularmind
DB_PASSWORD=                      # REQUIRED — generate: openssl rand -base64 24

# --- NextAuth ---
AUTH_SECRET=                      # REQUIRED — generate: openssl rand -base64 32
