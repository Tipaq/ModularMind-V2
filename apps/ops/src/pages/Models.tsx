export default function Models() {
  return (
    <div>
      <h1 className="text-2xl font-bold">Models</h1>
      <p className="mt-2 text-muted-foreground">
        Model catalog, pull status, and provider configuration.
      </p>
      {/* TODO: Model cards, pull progress, provider status */}
    </div>
  );
}
