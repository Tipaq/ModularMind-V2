export default function Configuration() {
  return (
    <div>
      <h1 className="text-2xl font-bold">Configuration</h1>
      <p className="mt-2 text-muted-foreground">
        System settings, LLM providers, MCP servers, and integrations.
      </p>
      {/* TODO: Settings panels, provider key management, MCP config */}
    </div>
  );
}
